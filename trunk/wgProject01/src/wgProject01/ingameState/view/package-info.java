/**
 * The MVC (model view controller) pattern is used to divide the game into smaller modules.
 * This is the view module, which contains the the games visualization and user inputs.
 * 
 * It reads the model module to know what to visualize and
 * maps user inputs to actions of the control module. 
 * 
 * @author oli
 *
 */
package wgProject01.ingameState.view;