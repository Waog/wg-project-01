/**
 * The MVC (model view controller) pattern is used to divide the game into smaller modules.
 * This is the control module, which contains the game logic. 
 * 
 * It manipulates the model module and receives commands from the view module if user inputs occur.
 * 
 * @author oli
 *
 */
package wgProject01.ingameState.control;